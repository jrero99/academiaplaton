# Copia este archivo a .env y rellena los valores.
# Cada workspace puede tener además su propio .env (apps/backend/.env, apps/frontend/.env).

# Backend
DATABASE_URL="mysql://root:password@localhost:3306/academiaplaton"
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Frontend (también vive en apps/frontend/.env)
VITE_API_URL=http://localhost:3000/api
